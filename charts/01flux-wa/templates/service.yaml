apiVersion: v1
kind: Service
metadata:
  name: {{ include "01flux-wa.fullname" . }}
  labels:
    {{- include "01flux-wa.labels" . | nindent 4 }}
    # Marks this Service as the single scrape target. Deliberately NOT in `01flux-wa.labels`: the
    # headless Service must not carry it, or the ServiceMonitor matches both again. The headless
    # Service exists for StatefulSet DNS and sets publishNotReadyAddresses, so scraping it also
    # produces `up=0` for a pod that is still starting.
    01flux-wa.io/scrape-target: "primary"
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "01flux-wa.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
