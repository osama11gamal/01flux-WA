apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "01flux-wa.fullname" . }}
  labels:
    {{- include "01flux-wa.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
